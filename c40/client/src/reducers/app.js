import {ADD_DATA, LOAD_DATA} from '../constants/ActionTypes'

export default function data(state = [], action){
  switch (action.type) {
    case ADD_DATA:
    return state

    default:
    return state

  }
}
